name: Build and Push

on:
  push:
    branches:
      - "**"
  # pull_request:
  #   branches:
  #     - "**"

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    timeout-minutes: 300

    steps:
      - name : display infomation
        run: |
          echo '1. '${{ github.head_ref }}
          echo '2. '${{ github.base_ref }}
        shell: bash -xe {0}


